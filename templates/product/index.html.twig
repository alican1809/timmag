{% extends 'base.html.twig' %}

{% block title %}Tim Stores - Produits
{% endblock %}

{% block body %}

	{# <br>
		<br>
		<h1>Filtre</h1>
		{{form(form)}}  #}
	{% for product in products %}

		<div></div>
	{% endfor %}
	<br>
	<br>
	<h1>Produits</h1>
	{% for product in products %}
		<div>

			{{product.category}}
			<a href="{{ path('app_product' , {'slug' : product.slug }) }}">
				<img src="/uploads/{{product.illustration}}" alt="">
		</a>
			<div><a href="{{ path('app_product' , {'slug' : product.slug }) }}">{{product.name}}</a></div>
			{{product.subtitle}}
			{{product.description}}
			<div>{{ (product.price/100)|number_format(2,',','.') }}
				€</div>
	
			<a href="{{ path('app_add_cart', {id: product.id}) }}">Ajouter</a>                 
			<a href="{{ path('app_product' , {'slug' : product.slug }) }}">detail</a>

		</div>
	{% endfor %}


{% endblock %}
